import { SearchFilterTicketPipe } from './search-filter-ticket.pipe';

describe('SearchFilterTicketPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterTicketPipe();
    expect(pipe).toBeTruthy();
  });
});
