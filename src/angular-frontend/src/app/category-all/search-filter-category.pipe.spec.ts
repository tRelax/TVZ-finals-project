import { SearchFilterCategoryPipe } from './search-filter-category.pipe';

describe('SearchFilterCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
