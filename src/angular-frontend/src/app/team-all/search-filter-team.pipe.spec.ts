import { SearchFilterTeamPipe } from './search-filter-team.pipe';

describe('SearchFilterTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
