import { SearchFilterUserPipe } from './search-filter-user.pipe';

describe('SearchFilterUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterUserPipe();
    expect(pipe).toBeTruthy();
  });
});
